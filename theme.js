export const colors = {
  background: "#14201A",     // vert-noir bouteille, fond principal
  surface: "#1E2B22",        // cards
  surfaceAlt: "#243329",     // éléments imbriqués (barres, badges)
  border: "#2C3A30",

  accent: "#C9A227",         // or huile d'olive — valeurs clés
  sage: "#9FAF98",           // argenté feuille d'olivier — texte discret

  success: "#7FA65C",        // vert olive mat — sain
  clay: "#B76E4D",           // terre cuite — stress hydrique
  alert: "#C4453A",          // rouge terracotta — maladie
  info: "#6E9CA6",           // bleu-teal — irrigation

  text: "#F3F1E9",           // blanc chaulé
  textMuted: "#B7C2B2",      // sage clair
};

export const spacing = {
  xs: 5,
  sm: 10,
  md: 15,
  lg: 20,
  xl: 30,
};

export const radius = {
  sm: 12,
  md: 16,
  lg: 20,
  xl: 28,
};

export const shadow = {
  sm: {
    shadowColor: "#000",
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.25,
    shadowRadius: 6,
    elevation: 3,
  },
  md: {
    shadowColor: "#000",
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.3,
    shadowRadius: 10,
    elevation: 5,
  },
};